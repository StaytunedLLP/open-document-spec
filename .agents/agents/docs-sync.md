# Subagent: docs-sync

**Use when:** after a spec or CLI product-surface change, keep all human/agent surfaces aligned.

**Scope:** `docs/guide/**`, `docs/other-specs/**`, `skills/ods/**`, `app-web/**` (nav/sitemap/llms), `AGENTS.md`, `CHANGELOG.md`.

**Process:** follow `.agents/skills/release-docs/SKILL.md` checklist end-to-end.
