name: 'OpenDocify (ODC) CI Action'
description: 'Install OpenDocify CLI (odc) and validate ODS documentation graph consistency, frontmatter compliance, and index freshness.'
author: 'Staytuned LLP'
branding:
  icon: 'file-text'
  color: 'blue'

inputs:
  version:
    description: 'ODS CLI release version tag to install (e.g., "latest", "v0.1.24", "0.1.24").'
    required: false
    default: 'latest'
  command:
    description: 'ODS subcommand to execute ("lint", "index-check", "doctor", "fmt-check", "bench", "export", or "" for setup-only).'
    required: false
    default: 'lint'
  path:
    description: 'Target ODS workspace directory path relative to repo root.'
    required: false
    default: '.'
  level:
    description: 'Lint compliance level for "odc ods lint" ("1" or "3").'
    required: false
    default: '3'
  format:
    description: 'Output format for CLI diagnostics ("text" or "json").'
    required: false
    default: 'text'
  token:
    description: 'GitHub token for fetching release binaries from private or public GitHub Releases.'
    required: false
    default: ${{ github.token }}
  annotate:
    description: 'Enable GitHub inline PR code annotations for lint errors via problem matcher ("true" or "false").'
    required: false
    default: 'true'
  extra-args:
    description: 'Additional raw command line arguments to pass to the ODS CLI.'
    required: false
    default: ''

outputs:
  ods-version:
    description: 'Installed ODS CLI version.'
    value: ${{ steps.setup-unix.outputs.ods-version || steps.setup-win.outputs.ods-version }}
  ods-path:
    description: 'Absolute path to the installed odc/ods CLI binary.'
    value: ${{ steps.setup-unix.outputs.ods-path || steps.setup-win.outputs.ods-path }}
  odc-version:
    description: 'Installed OpenDocify CLI version (alias of ods-version).'
    value: ${{ steps.setup-unix.outputs.odc-version || steps.setup-win.outputs.odc-version || steps.setup-unix.outputs.ods-version || steps.setup-win.outputs.ods-version }}
  odc-path:
    description: 'Absolute path to the installed odc binary (alias of ods-path).'
    value: ${{ steps.setup-unix.outputs.odc-path || steps.setup-win.outputs.odc-path || steps.setup-unix.outputs.ods-path || steps.setup-win.outputs.ods-path }}

runs:
  using: 'composite'
  steps:
    - name: Setup and Run ODS Action (Linux / macOS)
      id: setup-unix
      if: runner.os != 'Windows'
      shell: bash
      env:
        INPUT_VERSION: ${{ inputs.version }}
        INPUT_COMMAND: ${{ inputs.command }}
        INPUT_PATH: ${{ inputs.path }}
        INPUT_LEVEL: ${{ inputs.level }}
        INPUT_FORMAT: ${{ inputs.format }}
        INPUT_TOKEN: ${{ inputs.token }}
        INPUT_ANNOTATE: ${{ inputs.annotate }}
        INPUT_EXTRA_ARGS: ${{ inputs.extra-args }}
      run: |
        SCRIPT_PATH="${{ github.action_path }}/src/action/scripts/setup.sh"
        if [ ! -f "${SCRIPT_PATH}" ]; then
          SCRIPT_PATH="${{ github.action_path }}/scripts/setup.sh"
        fi
        "${SCRIPT_PATH}"

    - name: Setup and Run ODS Action (Windows)
      id: setup-win
      if: runner.os == 'Windows'
      shell: pwsh
      env:
        INPUT_VERSION: ${{ inputs.version }}
        INPUT_COMMAND: ${{ inputs.command }}
        INPUT_PATH: ${{ inputs.path }}
        INPUT_LEVEL: ${{ inputs.level }}
        INPUT_FORMAT: ${{ inputs.format }}
        INPUT_TOKEN: ${{ inputs.token }}
        INPUT_ANNOTATE: ${{ inputs.annotate }}
        INPUT_EXTRA_ARGS: ${{ inputs.extra-args }}
      run: |
        $ScriptPath = Join-Path "${{ github.action_path }}" "src\action\scripts\setup.ps1"
        if (-not (Test-Path $ScriptPath)) {
          $ScriptPath = Join-Path "${{ github.action_path }}" "scripts\setup.ps1"
        }
        & $ScriptPath
